<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 为这个mapper指定一个唯一的namespace，namespace的值习惯上设置成包名+sql映射文件名，这样就能够保证namespace的值是唯一的
例如namespace="me.gacl.mapping.userMapper"就是me.gacl.mapping(包名)+userMapper(userMapper.xml文件去除后缀)
 -->
<mapper namespace="mapping.userMapper">
    <!-- 映射关系 -->
    <resultMap id="userResult" type="User">
        <id property="uiId" column="id"/>
        <result property="uiAccount" column="username"/>
        <result property="uiPassword" column="password"/>
        <result property="uiName" column="name"/>
        <result property="uiGender" column="gender"/>
        <result property="uiPhoneNum" column="phone"/>
        <result property="uiMailAddr" column="email"/>
        <result property="uiCreatedTime" column="create_time"/>
        <result property="uiWeight" column="role"/>
        <result property="uiIsblacked" column="blacked"/>
    </resultMap>

    <select id="getUser" parameterType="int" resultMap="userResult">
        SELECT * FROM sys_user WHERE id=#{id}
    </select>
</mapper>