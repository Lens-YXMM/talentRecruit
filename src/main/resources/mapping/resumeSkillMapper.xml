<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapping.resumeSkillMapper">
    <!-- 映射关系 -->
    <resultMap id="resumeSkillResult" type="ResumeSkill">
        <id property="id" column="id"/>
        <result property="resumeId" column="resume_id"/>
        <result property="name" column="name"/>
        <result property="level" column="level"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getSkillByResumeId" parameterType="int" resultMap="resumeSkillResult">
        SELECT * FROM resume_skill WHERE resume_id=#{resumeId}
    </select>
</mapper>